const main = require('./main');

describe('main', () => {
   test('test base', () => {
      expect(true).toBe(true);
   });

   test('should show values for process', () => {
      main.
   });
});